* MRVA download
  This repository provides two scripts to download CodeQL databases
  given a partial url path of the form
  : /repos/protocolbuffers/protobuf/code-scanning/codeql/databases/cpp
  
  The lists are from the same source as the CodeQL MRVA plugin.  They
  are the top 1000 projects in cpp/java/python respectively.

  Following 
  https://docs.github.com/en/rest/code-scanning/code-scanning?apiVersion=2022-11-28#get-a-codeql-database-for-a-repository
  gives the two scripts [[./bin/download]] and [[./bin/download-all]]

  The lists to use are external; their converted forms are in =repos-*=.
    # ../mrva-top-repos/cpp/top.json
    # ../mrva-top-repos/python/top.json
    # ../mrva-top-repos/java/top.json
    # - lists with partial download syntax
    # #+BEGIN_SRC sh 
    #   jq '.repositories | .[] |.name'<  ../mrva-top-repos/cpp/top.json 
    #   jq '.repositories | .[] |.name'<  \
    #      ../mrva-top-repos/cpp/top.json |\
    #       sed 's|"\(.*\)/\(.*\)"|\1 \2|g;'
    #   S_LANGUAGE=cpp
    #   jq '.repositories | .[] |.name'<  \
    #      ../mrva-top-repos/$S_LANGUAGE/top.json |\
    #       sed 's|"\(.*\)/\(.*\)"|/repos/\1/\2/code-scanning/codeql/databases/'$S_LANGUAGE'|g;' >\
    #           repos-$S_LANGUAGE

    #   for S_LANGUAGE in cpp python java; do
    #       jq '.repositories | .[] |.name'<  \
    #          ../mrva-top-repos/$S_LANGUAGE/top.json |\
    #           sed 's|"\(.*\)/\(.*\)"|/repos/\1/\2/code-scanning/codeql/databases/'$S_LANGUAGE'|g;' >\
    #               repos-$S_LANGUAGE
    #   done
    # #+END_SRC

  The download is done via 
    #+BEGIN_SRC sh 
      cd /mnt/common/large-gpt-data/mrva-open-source-download/
      ./bin/download-all repos-cpp && ./bin/download-all repos-java && ./bin/download-all repos-python
    #+END_SRC


  If you run across a rate limit problem, there will be very small DBs.  Those can
  be found and pruned via e.g.,
  #+BEGIN_SRC sh 
    export FIND=gfind

    export FIND=find

    # Count DBs
    $FIND repos -name db.zip -size +2k -printf "%k\t\t%p\n"  | sort -n | wc -l

    # Count small DBs
    $FIND repos -name db.zip -size -2k -printf "%k\t\t%p\n"  | sort -n | wc -l

    # Remove invalid downloads
    $FIND repos -name db.zip -size -2k -printf "%k\t\t%p\n"  |sort -n |\
	while read size file
	do
	    rm "$file"
	done

  #+END_SRC

  After that, just re-run =./bin/download-all=.
  
* Convert to CSV
  - set up [[file:~/work-gh/sarif-cli/notes/quickstart.org::*sarif-cli quickstart][sarif-cli quickstart]]
  - prepare sarif files
    #+BEGIN_SRC sh 
      cd /mnt/common/large-gpt-data/mrva-open-source-download

      # starting list of sarif files
      mkdir -p ts
      find . -name '*.sarif' > ts/sarif-files

      # Build a list of actual files to feed the runner
      while read -r sf; do
          if grep -q versionControlProvenance "$sf"; then
              echo "$sf"
          else
              out="${sf}.1.sarif"
              sarif-insert-vcp "$sf" > "$out"
              echo "$out"
          fi
      done < ts/sarif-files | tee ts/sarif-files.final
    #+END_SRC
  - Feed them all to the runner
    #+BEGIN_SRC sh 
      pushd /mnt/common/large-gpt-data/mrva-open-source-download

      # 
      [ -f ts/sarif-files.final ] || echo "need file list"

      # clean up
      find . -name "*.sarif.scanlog" -exec rm {} \;


      # 
      sarif-extract-scans-runner --input-signature CLI - \
                                 > /dev/null \
                                 < ts/sarif-files.final &

      # look for errors
      rg -l -g "*.scanlog" -e 'Traceback.*most recent call last.*:' repos/

      # examine them
      tail -n 10 $(rg -l -g "*.scanlog" -e 'Traceback.*most recent call last.*:' repos/ |head -20)


    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text
      Option 2: Normalize input before typing

      If you want to keep the schema frozen but auto-map the field names:

      def normalize_run_fields(run):
          if "conversion" in run and "columnKind" not in run:
              run["columnKind"] = "utf8"
          if "conversion" in run and "properties" not in run:
              run["properties"] = run.pop("conversion")
          return run

      Then run:

      normalize_run_fields(json_data["runs"][0])
    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text
      Traceback (most recent call last):
        File "/mnt/common/home/hohn/work-gh/sarif-cli/.venv-ubuserv/bin/sarif-extract-scans", line 123, in <module>
          sarif_struct = signature.fillsig(args, sarif_struct, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 364, in fillsig
          return fillsig_list(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 354, in fillsig_list
          final.append(fillsig(args, el, context))
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 364, in fillsig
          return fillsig_list(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 354, in fillsig_list
          final.append(fillsig(args, el, context))
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 368, in fillsig
          raise Exception("Unknown element type", t, elem)
      Exception: ('Unknown element type', <class 'NoneType'>, None)
      ~
    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text

    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text
    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text
    #+END_SRC
