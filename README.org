* MRVA download
  This repository provides two scripts to download CodeQL databases
  given a partial url path of the form
  : /repos/protocolbuffers/protobuf/code-scanning/codeql/databases/cpp
  
  The lists are from the same source as the CodeQL MRVA plugin.  They
  are the top 1000 projects in cpp/java/python respectively.

  Following 
  https://docs.github.com/en/rest/code-scanning/code-scanning?apiVersion=2022-11-28#get-a-codeql-database-for-a-repository
  gives the two scripts [[./bin/download]] and [[./bin/download-all]]

  The lists to use are external; their converted forms are in =repos-*=.
    # ../mrva-top-repos/cpp/top.json
    # ../mrva-top-repos/python/top.json
    # ../mrva-top-repos/java/top.json
    # - lists with partial download syntax
    # #+BEGIN_SRC sh 
    #   jq '.repositories | .[] |.name'<  ../mrva-top-repos/cpp/top.json 
    #   jq '.repositories | .[] |.name'<  \
    #      ../mrva-top-repos/cpp/top.json |\
    #       sed 's|"\(.*\)/\(.*\)"|\1 \2|g;'
    #   S_LANGUAGE=cpp
    #   jq '.repositories | .[] |.name'<  \
    #      ../mrva-top-repos/$S_LANGUAGE/top.json |\
    #       sed 's|"\(.*\)/\(.*\)"|/repos/\1/\2/code-scanning/codeql/databases/'$S_LANGUAGE'|g;' >\
    #           repos-$S_LANGUAGE

    #   for S_LANGUAGE in cpp python java; do
    #       jq '.repositories | .[] |.name'<  \
    #          ../mrva-top-repos/$S_LANGUAGE/top.json |\
    #           sed 's|"\(.*\)/\(.*\)"|/repos/\1/\2/code-scanning/codeql/databases/'$S_LANGUAGE'|g;' >\
    #               repos-$S_LANGUAGE
    #   done
    # #+END_SRC

  The download is done via 
    #+BEGIN_SRC sh 
      cd /mnt/common/large-gpt-data/mrva-open-source-download/
      ./bin/download-all repos-cpp && ./bin/download-all repos-java && ./bin/download-all repos-python
    #+END_SRC


  If you run across a rate limit problem, there will be very small DBs.  Those can
  be found and pruned via e.g.,
  #+BEGIN_SRC sh 
    export FIND=gfind

    export FIND=find

    # Count DBs
    $FIND repos -name db.zip -size +2k -printf "%k\t\t%p\n"  | sort -n | wc -l

    # Count small DBs
    $FIND repos -name db.zip -size -2k -printf "%k\t\t%p\n"  | sort -n | wc -l

    # Remove invalid downloads
    $FIND repos -name db.zip -size -2k -printf "%k\t\t%p\n"  |sort -n |\
	while read size file
	do
	    rm "$file"
	done

  #+END_SRC

  After that, just re-run =./bin/download-all=.
  
* Convert to CSV
  - set up [[file:~/work-gh/sarif-cli/notes/quickstart.org::*sarif-cli quickstart][sarif-cli quickstart]]
  - prepare sarif files
    #+BEGIN_SRC sh 
      cd /mnt/common/large-gpt-data/mrva-open-source-download

      # starting list of sarif files
      mkdir -p ts
      find . -name '*.sarif' > ts/sarif-files

      # Build a list of actual files to feed the runner
      while read -r sf; do
          if grep -q versionControlProvenance "$sf"; then
              echo "$sf"
          else
              out="${sf}.1.sarif"
              sarif-insert-vcp "$sf" > "$out"
              echo "$out"
          fi
      done < ts/sarif-files | tee ts/sarif-files.final
    #+END_SRC
  - Feed them all to the runner
    #+BEGIN_SRC sh 
      pushd /mnt/common/large-gpt-data/mrva-open-source-download

      # 
      [ -f ts/sarif-files.final ] || echo "need file list"

      # clean up
      find . -name "*.sarif.scanlog" -exec rm {} \;


      # 
      sarif-extract-scans-runner --input-signature CLI - \
                                 > /dev/null \
                                 < ts/sarif-files.final &

      # look for errors
      rg -l -g "*.scanlog" -e 'Traceback.*most recent call last.*:' repos/

      # examine them
      tail -n 10 $(rg -l -g "*.scanlog" -e 'Traceback.*most recent call last.*:' repos/ |head -20)


    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text
      Option 2: Normalize input before typing

      If you want to keep the schema frozen but auto-map the field names:

      def normalize_run_fields(run):
          if "conversion" in run and "columnKind" not in run:
              run["columnKind"] = "utf8"
          if "conversion" in run and "properties" not in run:
              run["properties"] = run.pop("conversion")
          return run

      Then run:

      normalize_run_fields(json_data["runs"][0])
    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text
      Traceback (most recent call last):
        File "/mnt/common/home/hohn/work-gh/sarif-cli/.venv-ubuserv/bin/sarif-extract-scans", line 123, in <module>
          sarif_struct = signature.fillsig(args, sarif_struct, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 364, in fillsig
          return fillsig_list(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 354, in fillsig_list
          final.append(fillsig(args, el, context))
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 364, in fillsig
          return fillsig_list(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 354, in fillsig_list
          final.append(fillsig(args, el, context))
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 362, in fillsig
          return fillsig_dict(args, elem, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 345, in fillsig_dict
          final[key] = fillsig(args, val, context)
        File "/mnt/common/home/hohn/work-gh/sarif-cli/sarif_cli/signature.py", line 368, in fillsig
          raise Exception("Unknown element type", t, elem)
      Exception: ('Unknown element type', <class 'NoneType'>, None)
      ~
    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text

    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text
    #+END_SRC
  - update sarif-cli again
    #+BEGIN_SRC text
    #+END_SRC

* File system performance comparisons
  - ext4 :: 2m6
    #+BEGIN_SRC sh 
      cd ~/work-gh/mrva/mrva-open-source-download
      echo -e "\n=== EXT4 Extraction Test ==="
      time for db in repos/a*/*/code-scanning/codeql/databases/*/db.zip; do
      [ -f "$db" ] && unzip -q "$db" -d /dev/shm/test-ext4-$$ && rm -rf /dev/shm/test-ext4-$$
      done

      real	2m6.891s
      user	1m56.092s
      sys	0m10.589s
    #+END_SRC

  - zfs :: 2m9
    #+BEGIN_SRC sh 
      cd ~/work-gh/mrva/mrva-open-source-download

      echo -e "\n=== ZFS Extraction Test ==="
      time for db in repos-zfs/a*/*/code-scanning/codeql/databases/*/db.zip; do
          [ -f "$db" ] && unzip -q "$db" -d /dev/shm/test-zfs-$$ && rm -rf /dev/shm/test-zfs-$$
      done

      real	2m9.323s
      user	1m55.707s
      sys	0m13.389s
    #+END_SRC

  - parallel ext4 :: 0m22
    #+BEGIN_SRC sh 
      echo -e "\n=== Parallel EXT4 Test ==="
      time find repos/a*/*/code-scanning/codeql/databases/*/db.zip 2>/dev/null | \
        xargs -P 10 -I {} sh -c 'unzip -q {} -d /dev/shm/test-$$-$RANDOM && rm -rf /dev/shm/test-$$-$RANDOM'

      real	0m22.546s
      user	1m59.024s
      sys	0m13.303s
    #+END_SRC

  - parallel zfs :: 0m22
    #+BEGIN_SRC sh 
      echo -e "\n=== Parallel ZFS Test ==="
      time find repos-zfs/a*/*/code-scanning/codeql/databases/*/db.zip 2>/dev/null | \
        xargs -P 10 -I {} sh -c 'unzip -q {} -d /dev/shm/test-$$-$RANDOM && rm -rf /dev/shm/test-$$-$RANDOM'

      real	0m22.870s
      user	1m58.716s
      sys	0m16.716s
    #+END_SRC

  - Parallel XFS Test :: 0m22
    #+BEGIN_SRC sh 
      cd ~/work-gh/mrva/mrva-open-source-download

      echo -e "\n=== Parallel XFS Test ==="
      time find repos-xfs/a*/*/code-scanning/codeql/databases/*/db.zip 2>/dev/null | \
          xargs -P 10 -I {} sh -c 'unzip -q {} -d /dev/shm/test-$$-$RANDOM && rm -rf /dev/shm/test-$$-$RANDOM'

      real	0m22.327s
      user	1m59.177s
      sys	0m12.574s

    #+END_SRC

  - [ ] Sequential XFS Test :: who cares
    #+BEGIN_SRC sh 
      cd ~/work-gh/mrva/mrva-open-source-download

      echo -e "\n=== XFS Extraction Test ==="
      time for db in repos-xfs/a*/*/code-scanning/codeql/databases/*/db.zip; do
          [ -f "$db" ] && unzip -q "$db" -d /dev/shm/test-xfs-$$ && rm -rf /dev/shm/test-xfs-$$
      done
    #+END_SRC
* Add sharded tree
  1. repos/
     - Canonical source
     - This is where the actual state of the world lives.
     - All writes go here
     - Treat repos/ like a normal filesystem tree.

  2. sharded/
     - Physical storage mirror.
     - Never touched by humans or algorithmic tools directly.
     - Never edit it directly.

  3. logical/
     - *Read-only*, navigational view of the sharded tree.
     - A clean semantic tree for:
       + ls
       + find
       + grep
       + codeql
       + tools / scripts
       + browsing
       + interactive exploration
       + testing algorithms

     - You do not write here.
     - You do not unzip here.
     - You do not create new repos here.
     - You do not modify files here.

     - It is the “UI” of your filesystem

  4. Run read-only or read-majority tools inside logical/.
     But writes must be directed to repos/.  *Below the level of repos/org/,
     the subtree is identical in both logical/ and repos/.* 
     

  In summary:
  - =logical/= is the place for read-heavy operations
  - once all info is collected, switch to =repos/= for *write* operations
  - the paths, as strings, are identical below =repos/= and =logical/=, e.g.
    #+BEGIN_SRC sh
      $ ls logical/lz4
      lz4  lz4-java  repos

      0:$ ls repos/lz4
      lz4  lz4-java
    #+END_SRC




  #+BEGIN_SRC sh 
    # Common variables for reference
    ORIGINAL="repos"             # original canonical tree
    SHARDED="sharded"            # physical sharded storage
    LOGICAL="logical"            # logical namespace you will test
    SHARD_COUNT=4096             # number of shards (hex 000–FFF)
    MAP="shard-map.txt"

    # 
    # produce sharded tree
    cd ~/work-gh/mrva/mrva-open-source-download

    sh bin/shard_allocator.sh > shard-map.txt

    sh bin/hardlink_mirror.sh

    sh bin/logical_tree_generator.sh 

    #
    # compare / test trees
    # slow
    sh bin/verify_repos_to_sharded.sh
    # or
    sh bin/verify_repos_to_sharded_fast.sh 

    sh bin/verify_logical_symlinks.sh
    sh bin/verify_directory_structure.sh
  #+END_SRC

* Update sharded tree
  #+BEGIN_SRC sh 
    echo "=== DETECT NEW ORGS ==="
    sh ./bin/assign_new_orgs.sh

    echo "=== MIRROR UPDATED ORGS ==="
    sh ./bin/mirror_new_orgs.sh

    echo "=== UPDATE LOGICAL TREE ==="
    sh ./bin/logical_update.sh

    echo "=== DONE ==="

  #+END_SRC
